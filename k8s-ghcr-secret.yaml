apiVersion: v1
kind: Secret
metadata:
  name: ghcr-auth
type: kubernetes.io/dockerconfigjson
data:
  # This is a placeholder. You need to generate this with the command:
  # kubectl create secret docker-registry ghcr-auth --docker-server=ghcr.io --docker-username=YOUR_GITHUB_USERNAME --docker-password=YOUR_GITHUB_TOKEN --docker-email=YOUR_EMAIL -o yaml --dry-run=client
  # Then copy the .dockerconfigjson value here
  .dockerconfigjson: BASE64_ENCODED_DOCKER_CONFIG 